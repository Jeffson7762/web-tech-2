import { Routes } from '@angular/router';
import { prelimExam } from './prelim-exam/prelim-exam.component';

export const routes: Routes = [
    {
        path: 'prelim-exam',
        component: prelimExam
    }
];
